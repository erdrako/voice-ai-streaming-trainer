# Intentionally empty.
#
# A future real implementation would add project_id, region, zone, machine_type,
# disk_size_gb and allowed_admin_cidrs here. They are omitted now because this
# reference must not create billable GCP resources.
